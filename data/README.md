# Data

This folder contains data files used for simulations and analysis.

## Structure

- Raw data should be stored in subdirectories organized by experiment or simulation
- Processed data can be stored separately from raw data
- Consider using `.gitignore` to exclude large data files from version control
