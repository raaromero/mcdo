# Q&A log — questions asked, answers given, and where to verify each

Running record so each answer can be checked against a specific artifact.
Session of 2026-06-14 (Phase 6a Monte-Carlo: launcher, sampling, pulsed).

| # | Question | Answer (short) | Verify here |
|---|----------|----------------|-------------|
| 1 | Is everything complete before the Monte Carlo? | Yes — clear-limit gate, vector MC, sampling/N, voxel, launcher proof; 25 tests pass. | `docs/review/06_monte_carlo/review_06.pdf`; `pytest tests/` |
| 2 | Plot for sufficient N and voxel size? | N≈3×10³ for 1% RMS (axicon 10⁴); voxel dx=dy≤λ/4NA=234 nm, dz≤681 nm. | `output/06_monte_carlo/study_sampling.png` |
| 3 | Proof the initialization (direction, location, uniform/Gaussian) is accurate for scattering? | Direction, azimuth, input profile, and the resulting field all match; certified pre-scattering. | `verify_launcher.png`, `verify_launcher_intensity.png`; theory §3,§6 |
| 4 | Compare the sampled distribution vs expected (uniform/Gaussian)? | θ-density = Debye–Wolf integrand (KS=9×10⁻⁴); recovered A(θ) flat/Gaussian. | `verify_launcher.png` panels (1),(2),(4) |
| 5 | Intensities after MC launching vs expected? | Match on transverse+axial cuts and x–z slice, RMS 6–8×10⁻⁴. | `verify_launcher_intensity.png` |
| 6 | What is the distribution of initial directions? | Cone θ∈[0,α=38°], density ∝ |A|√cosθ sinθ; φ uniform; |k|=nk₀ (spherical cap). | `verify_launcher.png` panel (1) |
| 7 | 2-D slices with both heat map and Romallosa iso-contours? | Adopted as the standard; gates use heat map + labeled contours. | `verify_launcher_intensity.png` (gates) |
| 8 | Don't import concepts from external codebases. | Honored — all within our own RW/Debye–Wolf framework. | (policy) |
| 9 | Where are the review PDFs? | Built the Phase-6 one (was missing). | `docs/review/06_monte_carlo/review_06.pdf` |
| 10 | The Phase-6a plots aren't clear. | Reworked: smoothed pupil maps, normalized FWHM panels, bigger fonts, separated concerns. | `study_sampling.png`, `verify_launcher*.png` |
| 11 | Visual flowchart / optical schematic for each model since Romallosa? | One card per phase: pupil → focusing cone → focal signature + result. | `output/00_overview/models_schematic.png` (review_06 p.1) |
| 12 | How do we know the launch is correct beyond matching distributions? Straight lines? Constraints on directions? | First-principles: plane-wave decomposition is Debye–Wolf; weights forced by NA + aplanatic factor; unbiased estimator. Straight lines (wavefront normals). 5 constraints. | theory §3 `06_monte_carlo.md`; this answer in chat |
| 13 | Why is "uniform" missing in the azimuth panel? | Both inputs are φ-uniform → markers overlapped; fixed with distinct markers + labeled line. | `verify_launcher.png` panel (3) |
| 14 | Any bad results? | None broken; soft/flagged: axicon J₀² 4–8% (SPA), pulsed S non-monotonic, annular DOF high-NA, null-floor plateau, FWHM jitter, Fig-3 3%. | `docs/theory/FLAGS.md`; reviews 04,05 |
| 15 | How to verify axicon J₀² and pulsed S? Is axicon a focus extended along z? | Axicon = yes, Bessel focal segment. Both verified (β-scan; dense-τ+N_freq+vector). | `axicon_validity.png`, `pulsed_S_diagnostic.png` |
| 22 | Run axicon with more N, then keep going as long as valid. | More N: RMS↓ to floor ≈4e-4 at N=10⁶ (β=20). Push β: J₀² SPA error 0.134→0.006 (β 5→2560), MC=det ≤0.019 (bias-free). Limits: N_theta(math) β>2560; physical Debye (f≈2mm) β≈160 binds first. | `output/04_axicon/axicon_validity.png`; `FLAGS.md` Flag 1 |
| 23 | Photons needed for each model (NA, input, Gaussian, annular, axicon)? | N(1% RMS): NA 0.1–1.2 ~200–2100; Gaussian α_t 0→4 0.9k→2.4k; annular ε 0→0.99 0.9k→1.3k; axicon β=0/5/10 ~0.9k but β=20≈40k, β=40≈370k. Cost is set by pupil-phase winding, not NA. | `output/06_monte_carlo/photons_per_model.png` |
| 24 | Why only ~10³ photons — is that because there's no scattering? | Yes. Clear-limit MC is importance-sampled coherent quadrature of a smooth 2-D integral → no rare events, ~10³ enough. Scattering → high-dim path space, incoherent halo over many voxels, exp(−μ_s L) ballistic fraction (rare event at depth) → 10⁶–10⁸. Motivates a hybrid (cheap coherent core + MC halo). | `review_06.pdf` §8; this chat |
| 25 | With scattering there's no expected result — how do I know it's good enough? Is everything set up? | Validate by known LIMITS (μ_s→0=clear field, Beer–Lambert ballistic, energy conservation, single-scatter Born, diffusion asymptote, MCML cross-check) + statistical convergence (mean±SD over seeds below tolerance, step/voxel converged). Infrastructure (launcher, kernel, coherent sum, wavelength) all set up & validated; 6b = wire them + build the harness. | `docs/theory/scattering_validation_plan.md` |
| 26 | For NO scattering, do we sample wavelengths too? | No need. In a clear (non-dispersive) medium the spectrum factors out, so the deterministic spectral sum (Eq. 10, `pulsed_intensity`) is exact and cheaper — per-photon λ sampling only adds MC variance to a dimension that doesn't need it. The λ-sampling MC (`mc_pulsed_intensity`, built + validated) is reserved for **scattering**, where μ_s(λ) couples λ to the path and the spectrum no longer factors out. | `mcdo/intensity.py`; `mcdo/montecarlo.py` `mc_pulsed_intensity` |
| 27 | Save multi-trial data (≥10) for ALL MC experiments, even no-scattering. | Built `mcdo.trials` (run/summarize/save/load) + `scripts/run_mc_trials.py`; K=10 saved per experiment (clear field, per-config, vector, launcher, pulsed) with mean/std/sem/seeds. Now the project standard; 6b built on it from the start. | `output/06_monte_carlo/trials/*.npz`; `mcdo/trials.py`; tests `test_trials.py` |
| 28 | Show profile mean ± SD for increasing N. | 2×3 (transverse/axial × N=10³/10⁴/10⁵), K=10 trials each; the ±1σ band tightens ∝1/√N (transverse mean σ 0.0058→0.0017→0.0005, axial 0.0076→0.0033→0.0006), collapsing onto the expected curve. Per-trial data saved. | `output/06_monte_carlo/profiles_vs_N.png`; `trials/profiles_vs_N.npz` |
| 29 | Build & validate the 6b scattering run (multi-trial). | Focused beam through slab (d=20µm, g=0.9), focus at back face: ballistic coherent core (amp exp(−μ_sL/2)) + diffuse halo. 3 rungs PASS — μ_s→0=clear, Beer-Lambert MC=⟨e^−μ_sL⟩ to <0.001, energy=1 to 2e-16. Ballistic focus dims (cone-avg BL, below e^−τ) & broadens ~5%; halo grows; backscatter ≤0.15. K=10 trials, data saved. | `output/06_monte_carlo/verify_mc_scatter.png`; `mcdo/focus_scatter.py`; `trials/scatter_*.npz` |
| 30 | How do I know the simulation is accurate? | Validation is a LADDER of independent checks, each vs something known: analytic limits (Airy/J₀²/Babinet/Linfoot, ≤1e-9–5e-3), conservation (energy 2e-16, ⟨cosθ⟩=g, Beer-Lambert), literature (Romallosa/Sheppard), internal consistency (MC=deterministic ≤8e-4; vector 6e-4; pulsed 3-5e-4), convergence (mean±SD, ∝1/√N), 35 regression tests. Honest gaps: scattering rungs 4-6 (Born/diffusion/MCML) and NO experiment/full-wave check yet. | `docs/ACCURACY.md` |
| 16 | How are directions sampled — is it sampling the focal intensity pattern? | No — inverse-transform on the **pupil** angular density; focal intensity is the output, never sampled. We initialize correctly without knowing it. | `direction_sampling_explained.md`; `verify_launcher.png` panel (1) |
| 17 | Is wavelength factored in? Does the focus move with λ? Plot the wavelength probability. | Launcher is monochromatic (k_c); pulsed should sample λ∝|E(ω)|². Focus position fixed at (0,0) for all λ; only the pattern scale ∝ λ (zeros move → pulsing fills them). λ-probability needs the |dω/dλ|∝ω² Jacobian. | `output/05_pulsed_pupils/spectral_sampling.png` |
| 18 | Pulsed S non-monotonicity: parameter or not enough N? Use E_x,E_y,E_z not one cut? | Real: smooth in τ (A), flat vs N_freq (B), present in y-cut AND x-cut AND full vector (C). Not a bug, not undersampling, not a cut artifact. | `output/05_pulsed_pupils/pulsed_S_diagnostic.png` |
| 19 | MC averaged intensity + standard deviation plot (per-run + SD band)? | Per-point mean ±1σ over K=40 runs, transverse+axial, N=2k & 20k; band shrinks ∝1/√N. | `output/06_monte_carlo/mc_variance.png` |
| 20 | Try the wavelength-probability MC (more physical); different uses? | Per-photon λ-sampling MC added; reduces to the deterministic Eq.10 in the clear limit. Uses: deterministic sum = clear/exact; per-photon λ = needed for pulsed + wavelength-dependent scattering. | `output/06_monte_carlo/verify_mc_pulsed.png` |
| 21 | Are we violating anything? Valid at high NA? Valid for axicons? | No fundamental violation. High NA: valid with the vector launcher (scalar is the only thing that "breaks", as designed). Axicons: valid (MC=deterministic RMS 1.8e-3); the 4–8% is the analytic J₀² model, and axicons just need more photons (variance, not bias). Assumes Debye (high Fresnel #) + no evanescent — both hold for focusing. | `direction_sampling_explained.md` §Validity; `verify_mc_vector.png`, `verify_mc_configs.png` |

| 31 | Born & Wolf is the go-to but daunting — where do I actually start? | B&W is the *reference*, not the on-ramp (consult §8.8/§8.6 only). Start: Novotny–Hecht ch. 3–4 (~60 pp, derives exactly our machinery incl. Eq. 3.66 = the launcher code) + Goodman ch. 3 (angular spectrum) → the two 1959 papers → B&W as lookup. Learn through `mcdo` itself (docstring→theory doc→re-derive→test). Mastery bar = 5 re-derivations, not 5 books. | `docs/LITERATURE.md` §0 |

*Add new rows as questions come up; keep the "verify here" column concrete.*
